import { createUseStyles } from "react-jss";

export const useStyle = createUseStyles({
  error: {
    color: "red",
    fontSize: "5rem",
    padding: "10px",
    textAlign: "center",
    textShadow: "red 1px 0 5px",
  },
});
