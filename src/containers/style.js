import { createUseStyles } from "react-jss";

export const useStyle = createUseStyles({
  headerTitle: {
    color: "white",
    fontSize: "4rem",
    padding: "5px",
    textAlign: "center",
    textShadow: "#fff 1px 0 10px",
  },
});
